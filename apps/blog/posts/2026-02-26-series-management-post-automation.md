---
title: "시리즈 관리와 글 생성 자동화"
date: "2026-02-26"
tags: ["automation", "script", "gemini"]
series: "playground"
published: true
---

# 시리즈 관리와 글 생성 자동화

내 블로그는 [벨로그](https://velog.io/@ejaman/posts)의 UX를 참조해서 만들었는데 벨로그에서 사용하는 것처럼 카테고리를 시리즈를 통해 관리했다.
그런데 나는 md 파일로 글을 작성하는데 시리즈 관리는 어떻게하지?

## 시리즈를 어떻게 관리할 것인가?

가장 먼저 든 생각은 파일 구조로 시리즈를 구분하는 방식이다.

```
posts/
├── playground/
│   ├── 2026-02-26-intro.md
│   └── 2026-02-27-turborepo.md
└── 2026-02-28-standalone.md
```

위와 같이 시리즈가 있다면 시리즈 폴더에 넣어주는 방식이다.
가장 직관적이고 구현이 편할 거 같다.

그러나 문제는 시리즈명을 폴더명으로 관리하다보면 오타, 대소문자 등으로 중복 시리즈가 생길 수 있고 나중에 시리즈명을 바꾸면 폴더를 옮기는 등 추후 관리가 복잡할 것 같았다.
이런게 쌓이면 쌓일수록 휴먼 에러 가능성이 높아질게 뻔해보였다.

## 시리즈를 중앙에서 관리하자

그래서 선택한 방식이 시리즈를 키 값으로 중앙에서 관리하는 것이다.
루트에 `series.json`을 만들어 시리즈를 등록한다.

```json
{
  "playground": {
    "title": "맞춤 플레이그라운드 만들기",
    "description": "Turborepo를 사용해서 블로그를 만들어보자"
  }
}
```

그리고 md 프론트매터에서 시리즈 키 값을 참조하는 방식이다.

```
---
title: "예시입니당"
series: "playground"
---
```

시리즈 제목이 바뀌어도 series.json 한 곳만 수정하면 되고 프론트매터를 하나씩 열어서 고칠 필요가 없어졌다.

Velite에서는 빌드 타임에 series 키를 읽어 series.json에서 제목을 가져온다.

```typescript
// velite.config.ts
const getSeriesTitle = (seriesKey: string): string => {
  const seriesPath = path.resolve(process.cwd(), "../../series.json");
  const seriesData = JSON.parse(fs.readFileSync(seriesPath, "utf-8"));
  return seriesData[seriesKey]?.title || seriesKey;
};

// transform에서 사용
seriesTitle: data.series ? getSeriesTitle(data.series) : undefined,
```

이를 통해 컴포넌트에서는 post.seriesTitle을 바로 쓸 수 있고, 키값과 제목을 분리해서 관리할 수 있다.

## 자동화해보자!

자 이제 최대한 자동화할 수 있는 것은 자동화해보자

매번 md 파일을 직접 만들고 프론트매터를 손으로 채우는 과정이 번거롭다. 그리고 파일명을 영문으로 생각하는 것도 너무 귀찮았다.
그래서 스크립트로 자동화하기로 해봤다.

```
pnpm create-post 실행
  → 시리즈 선택 (또는 새 시리즈 생성)
  → 제목 입력
  → 태그 입력
  → AI가 슬러그 자동 생성 (한글 제목인 경우만!)
  → md 파일 생성
```

그래서 터미널에서 보면

```
📚 현재 등록된 시리즈 목록:
  1. 맞춤 플레이그라운드 만들기 (playground)
  0. 시리즈 없음 (단발성 게시글)
  N. 새 시리즈 만들기
```

이런식으로 기존 시리즈 , 새로 생성, 시리즈 ㄴㄴ 이렇게 선택할 수 있도록 스크립트를 구성했다.

### AI 슬러그 생성

한글 제목은 URL에 그대로 쓰기 어렵기 때문에 영문 슬러그로 변환해야 한다. 규칙 기반 변환은 오타나 어색한 영문이 나올 수 있어서 Gemini API를 활용했다.

```typescript
const prompt = `Translate the following Korean blog title into a concise,
URL-friendly English slug (lowercase, hyphens only): "${title}"`;

const result = await model.generateContent(prompt);
slug = result.response
  .text()
  .trim()
  .toLowerCase()
  .replace(/[^a-z0-9-]/g, "");
```

토큰을 아껴야하니까 영문 제목은 AI 호출 없이 바로 처리한다.

```typescript
slug = title
  .toLowerCase()
  .trim()
  .replace(/\s+/g, "-")
  .replace(/[^a-z0-9-]/g, "");
```

#### 결과물

```
---
title: "시리즈 관리와 글 생성 자동화"
date: "2026-02-28"
tags: ["automation", "script", "gemini"]
series: "playground"
published: false
---
```

`published: false`를 기본값으로 생성되기 때문에 글을 완성하기 전에는 배포해도 노출되지 않는다.

## 개선해보자!

처음 스크립트를 작성하고 나서 몇 가지 버그와 불편한 점을 발견해 수정했다.

1. publish → published 통일 — Velite 스키마에서 published로 정의했는데 스크립트에서 publish로 생성하고 있었다. 필드명이 다르면 default(false)로 처리되어 동작은 하지만 의도가 명확하지 않아 통일했다.

2. series: null → 필드 생략 — 시리즈 없음을 선택하면 series: null이 프론트매터에 들어갔는데, Velite가 이를 문자열 "null"로 읽을 수 있어서 필드 자체를 생략하도록 변경했다.

3. 입력값 validation 추가 — 잘못된 시리즈 번호를 입력하면 undefined가 그대로 프론트매터에 들어가는 문제가 있었다. 범위 체크와 파일 중복 체크를 추가했다.

4. 콜백 중첩 제거 — 원래 rl.question 콜백이 중첩되어 있어서 가독성이 떨어졌다. question() 헬퍼 함수로 감싸 await로 순차 처리하도록 바꿨다.

```typescript
// 변경 전 — 콜백 중첩
rl.question("시리즈: ", (answer) => {
  rl.question("제목: ", async (title) => {
    rl.question("태그: ", (tags) => {
      // ...
    });
  });
});

// 변경 후 — await 순차 처리
const question = (prompt) =>
  new Promise((resolve) => rl.question(prompt, resolve));

const seriesAnswer = await question("시리즈: ");
const title = await question("제목: ");
const tagsInput = await question("태그: ");
```

## 남은 개선점

지금은 슬러그와 태그 정도만 자동화되어 있다. 나중에 글 작성을 완료하고 published: true로 바꾸기 전에, 본문 내용을 AI가 요약해 description 프론트매터를 자동으로 생성하는 스크립트도 추가할 예정이다.

글을 발행하는 워크플로우가 이렇게 되는 것을 목표로 하고 있다.

```
pnpm create-post          # 1. md 파일 생성
→ 글 작성 (published: false 상태)
→ pnpm gen-description    # 2. AI가 description 자동 생성
→ published: true로 변경  # 3. 발행
→ git push → Vercel 배포  # 4. 배포
```
