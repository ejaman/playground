{
  "name": "playground",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "create-post": "node scripts/create-post.mjs",
    "model-list": "node scripts/model-list.mjs"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.8.10",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@10.10.0",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/claude-code": "^2.1.85",
    "@google/generative-ai": "^0.24.1",
    "dotenv": "^17.3.1"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
